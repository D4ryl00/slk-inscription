# ─── HelloAsso ───────────────────────────────────────────────────────────────
# Identifiants API générés dans l'espace admin de l'association
# (Paramètres → Développeur / API). NE JAMAIS committer les vraies valeurs.
HELLOASSO_CLIENT_ID=
HELLOASSO_CLIENT_SECRET=
# Slug de l'organisation (dans l'URL HelloAsso: helloasso.com/associations/<slug>)
HELLOASSO_ORG_SLUG=association-stade-laurentin-karate
# "sandbox" pour les tests, "prod" en production
HELLOASSO_ENV=sandbox
# (Optionnel, PARTENAIRES) clé de signature de l'URL de notification. Si définie,
# le webhook vérifie le header x-ha-signature (HMAC-SHA256). Sinon, ignoré.
HELLOASSO_WEBHOOK_SIGNATURE_KEY=

# ─── Google Sheets + Drive (même compte de service) ──────────────────────────
# Coller le JSON de la clé du service account sur UNE seule ligne (échappé),
# ou l'encoder en base64 et préfixer par "base64:".
GOOGLE_SERVICE_ACCOUNT_JSON=
# ID du Google Sheet (partie entre /d/ et /edit dans l'URL)
GOOGLE_SHEET_ID=
# Nom de l'onglet où écrire les adhérents (par défaut la 1re feuille)
GOOGLE_SHEET_TAB=Feuille 1
# (Optionnel) ID du dossier Google Drive où déposer les photos d'identité
# (partie après /folders/ dans l'URL du dossier). Non défini → envoi des photos ignoré.
GOOGLE_DRIVE_PHOTOS_FOLDER_ID=

# ─── Google Drive OAuth (photos d'identité — compte GOOGLE DU CLUB) ───────────
# ⚠️ Un compte de service n'a PAS de quota de stockage : l'upload d'une photo
# échoue ("Service Accounts do not have storage quota"). On dépose donc la photo
# via OAuth2 en tant que compte Google du club (le fichier lui appartient, quota
# de 15 Go). Non renseigné → l'envoi des photos est ignoré (le reste fonctionne).
# 1) Créer un client OAuth "Application de bureau" dans le projet GCP.
# 2) `npm run drive:token` (avec CLIENT_ID/SECRET ci-dessous) → colle le refresh token.
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
GOOGLE_OAUTH_REFRESH_TOKEN=

# ─── Front ───────────────────────────────────────────────────────────────────
# URL publique du site (pour construire les returnUrl/backUrl/errorUrl du Checkout)
SITE_URL=http://localhost:8888
